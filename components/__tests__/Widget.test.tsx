describe('Widget (placeholder)', () => {
  it('should be tested with JSX render and interaction in a supported transform environment', () => {
    expect(true).toBe(true);
    // TODO: write tests for all FE components (Widget, Sidebar, Canvas)
  });
});
